name: Daily RepoSync

on:
  schedule:
    # Runs at 8:00 AM UTC every day
    - cron: '0 8 * * *'
  workflow_dispatch:  # Allows manual triggering

jobs:
  sync:
    name: Execute RepoSync with Docker Compose
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Verify Java version
        run: |
          echo "JAVA_HOME: $JAVA_HOME"
          java -version
          mvn -version

      - name: Build all services
        run: mvn clean package -DskipTests -Dcheckstyle.failOnViolation=false

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Verify Secrets Configuration
        run: |
          echo "Checking if required secrets exist..."
          
          if [ -z "${{ secrets.REPOSYNC_GITHUB_TOKEN }}" ]; then
            echo "❌ CRITICAL ERROR: REPOSYNC_GITHUB_TOKEN secret is EMPTY!"
            exit 1
          else
            echo "✅ REPOSYNC_GITHUB_TOKEN is set"
          fi
          
          if [ -z "${{ secrets.AZURE_OPENAI_API_KEY }}" ]; then
            echo "❌ CRITICAL ERROR: AZURE_OPENAI_API_KEY secret is EMPTY!"
            exit 1
          else
            echo "✅ AZURE_OPENAI_API_KEY is set"
          fi
          
          if [ -z "${{ secrets.AZURE_OPENAI_ENDPOINT }}" ]; then
            echo "❌ CRITICAL ERROR: AZURE_OPENAI_ENDPOINT secret is EMPTY!"
            exit 1
          else
            echo "✅ AZURE_OPENAI_ENDPOINT is set"
          fi
          
          if [ -z "${{ secrets.REPOSYNC_ORGANIZATION }}" ]; then
            echo "⚠️  WARNING: REPOSYNC_ORGANIZATION secret is EMPTY - will sync all repos"
          else
            echo "✅ REPOSYNC_ORGANIZATION is set"
          fi
          
          echo "✅ All critical secrets are configured"
          echo "ℹ️  Note: Using internal Docker Milvus (milvus-standalone) for CI runs"

      - name: Start Services with Docker Compose
        run: |
          echo "🐳 Starting all services with Docker Compose..."
          echo "This will build images and start services with proper health checks"
          
          # Create .env file with all necessary environment variables
          cat > .env << EOF
          REPOSYNC_GITHUB_TOKEN=${{ secrets.REPOSYNC_GITHUB_TOKEN }}
          REPOSYNC_ORGANIZATION=${{ secrets.REPOSYNC_ORGANIZATION }}
          REPOSYNC_FILTER_KEYWORD=${{ secrets.REPOSYNC_FILTER_KEYWORD }}
          AZURE_OPENAI_API_KEY=${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_ENDPOINT=${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=${{ secrets.AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT }}
          MILVUS_URI=${{ secrets.MILVUS_URI }}
          MILVUS_TOKEN=${{ secrets.MILVUS_TOKEN }}
          MILVUS_COLLECTION_NAME=${{ secrets.MILVUS_COLLECTION_NAME }}
          EOF
          
          echo "✅ .env file created with secrets"
          
          # Start only application services (using Zilliz Cloud for Milvus, not local containers)
          docker compose up -d \
            github-service \
            document-processor-service \
            embedding-service \
            milvus-service \
            orchestrator-service
          
          echo "✅ Docker Compose services started"

      - name: Wait for Services to be Healthy
        run: |
          echo "⏳ Waiting for Orchestrator Service to become healthy..."
          echo "Note: Orchestrator won't be healthy until all dependencies are healthy"
          
          # Wait up to 5 minutes for orchestrator to be healthy
          TIMEOUT=300
          ELAPSED=0
          
          while [ $ELAPSED -lt $TIMEOUT ]; do
            if curl -sf http://localhost:8086/actuator/health > /dev/null 2>&1; then
              echo "✅ Orchestrator Service is healthy!"
              echo ""
              echo "Verifying all services are responding:"
              
              echo "  1. GitHub Service (8081):"
              curl -sf http://localhost:8081/actuator/health && echo "     ✅ Healthy" || echo "     ❌ Failed"
              
              echo "  2. Document Processor Service (8082):"
              curl -sf http://localhost:8082/actuator/health && echo "     ✅ Healthy" || echo "     ❌ Failed"
              
              echo "  3. Embedding Service (8083):"
              curl -sf http://localhost:8083/actuator/health && echo "     ✅ Healthy" || echo "     ❌ Failed"
              
              echo "  4. Milvus Service (8084):"
              curl -sf http://localhost:8084/actuator/health && echo "     ✅ Healthy" || echo "     ❌ Failed"
              
              echo "  5. Orchestrator Service (8086):"
              curl -sf http://localhost:8086/actuator/health && echo "     ✅ Healthy" || echo "     ❌ Failed"
              
              echo ""
              echo "✅ All services are ready!"
              exit 0
            fi
            
            echo "Waiting for services to be ready... (${ELAPSED}s / ${TIMEOUT}s)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          
          echo "❌ Timeout reached! Services failed to become healthy within ${TIMEOUT}s"
          echo ""
          echo "=== Docker Compose Status ==="
          docker compose ps
          echo ""
          echo "=== Orchestrator Logs (last 100 lines) ==="
          docker compose logs --tail=100 orchestrator-service
          echo ""
          echo "=== Milvus Service Logs (last 100 lines) ==="
          docker compose logs --tail=100 milvus-service
          exit 1

      - name: Trigger Sync Job
        run: |
          echo "🚀 Triggering repository sync job..."
          
          HTTP_CODE=$(curl -s -w "%{http_code}" -X POST http://localhost:8086/api/orchestrator/sync \
            -H "Content-Type: application/json" \
            -o sync-result.json)
          
          echo "HTTP Response Code: $HTTP_CODE"
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "❌ Error: Sync API returned HTTP $HTTP_CODE"
            echo ""
            echo "Response body:"
            cat sync-result.json || echo "No response body"
            echo ""
            echo "=== Orchestrator Service Logs (last 200 lines) ==="
            docker compose logs --tail=200 orchestrator-service
            exit 1
          fi
          
          echo "Sync job initiated successfully"
          echo ""
          echo "Initial response:"
          cat sync-result.json
          echo ""
          
          # Give the sync job time to complete
          echo "⏳ Waiting for sync to complete (60 seconds)..."
          sleep 60

      - name: Check Sync Status
        run: |
          if [ ! -f sync-result.json ]; then
            echo "❌ Error: sync-result.json not found"
            exit 1
          fi
          
          echo "=== Sync Result JSON ==="
          cat sync-result.json
          echo "========================"
          echo ""
          
          # Extract status
          STATUS=$(jq -r '.status' sync-result.json)
          ERROR_MSG=$(jq -r '.errorMessage' sync-result.json)
          
          if [ "$STATUS" != "SUCCESS" ]; then
            echo "❌ Sync Failed!"
            echo "Status: $STATUS"
            echo "Error: $ERROR_MSG"
            echo ""
            echo "=== Debugging Information ==="
            echo ""
            echo "--- Orchestrator Logs (last 200 lines) ---"
            docker compose logs --tail=200 orchestrator-service
            echo ""
            echo "--- GitHub Service Logs (last 100 lines) ---"
            docker compose logs --tail=100 github-service
            echo ""
            echo "--- Embedding Service Logs (last 100 lines) ---"
            docker compose logs --tail=100 embedding-service
            echo ""
            echo "--- Milvus Service Logs (last 100 lines) ---"
            docker compose logs --tail=100 milvus-service
            echo ""
            exit 1
          fi
          
          # Success - show statistics
          REPOS=$(jq -r '.repositoriesProcessed // 0' sync-result.json)
          DOCS=$(jq -r '.documentsProcessed // 0' sync-result.json)
          CHUNKS=$(jq -r '.chunksCreated // 0' sync-result.json)
          VECTORS=$(jq -r '.vectorsStored // 0' sync-result.json)
          
          echo "✅ Sync Completed Successfully!"
          echo ""
          echo "📊 Statistics:"
          echo "  • Repositories processed: $REPOS"
          echo "  • Documents processed: $DOCS"
          echo "  • Chunks created: $CHUNKS"
          echo "  • Vectors stored: $VECTORS"

      - name: Save Docker Logs
        if: always()
        run: |
          echo "📝 Saving complete Docker logs..."
          docker compose logs > docker-compose-logs.txt
          echo "Logs saved to docker-compose-logs.txt"

      - name: Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sync-logs-and-results
          path: |
            sync-result.json
            docker-compose-logs.txt

      - name: Cleanup
        if: always()
        run: |
          echo "🧹 Cleaning up Docker Compose services..."
          docker compose down -v
          echo "✅ Cleanup complete"

